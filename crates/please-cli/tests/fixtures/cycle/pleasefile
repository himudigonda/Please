version = "0.3"

a: b
    @in src/input.txt
    @out dist/a.txt
    @isolation best_effort
    mkdir -p dist
    cat src/input.txt > dist/a.txt

b: a
    @in src/input.txt
    @out dist/b.txt
    @isolation best_effort
    mkdir -p dist
    cat src/input.txt > dist/b.txt
