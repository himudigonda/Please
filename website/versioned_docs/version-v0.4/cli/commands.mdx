---
sidebar_position: 1
---

# CLI Commands

```bash
broski list
broski <task>
broski run <task> --explain
broski graph <task> --format dot
broski doctor --no-repair
broski cache prune --max-size 512
```

Subcommands always take precedence over task names. Use `broski run <task>` when explicit resolution is needed.
